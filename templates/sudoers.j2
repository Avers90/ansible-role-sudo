# {{ ansible_managed }}
{% if item.name is defined %}
{% if item.type | default('user') == 'group' %}
%{{ item.name }} ALL=(ALL) {% if item.nopasswd | default(false) %}NOPASSWD:{% endif %}{{ item.commands | default('ALL') }}
{% else %}
{{ item.name }} ALL=(ALL) {% if item.nopasswd | default(false) %}NOPASSWD:{% endif %}{{ item.commands | default('ALL') }}
{% endif %}
{% endif %}
